[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "singlepact-reactor"
version = "0.1.0"
description = "Deterministic experiment kernel for transient executability locks"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
